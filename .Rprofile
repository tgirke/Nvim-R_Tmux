options(width=220)
# lib.loc="/usr/local/lib/R/site-library/"
#library(grDevices)
#X11.options(type="Xlib")
# default X11() setting

## For new vim-r (added May 2015)
if(interactive()){
   library(colorout)
   #library(setwidth)
   #options(vimcom.verbose = -1)
   #library(vimcom)
}

